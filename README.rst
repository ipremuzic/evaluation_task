Evaluation task
********

Implemetation was built for and tested on Nucleo F446RE development board.
This board has a user button which is mapped to btn0 alias and used as input and
there is also a user LED which is mapped to led0 alias and used as output. 